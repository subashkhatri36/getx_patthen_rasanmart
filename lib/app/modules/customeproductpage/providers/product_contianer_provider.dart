import 'package:get/get.dart';

import '../product_contianer_model.dart';

class ProductContianerProvider extends GetConnect {
  @override
  void onInit() {
    //  httpClient.defaultDecoder = (map) => ProductContianer.fromJson(map);
    // httpClient.baseUrl = 'YOUR-API-URL';
  }
}
