enum AuthenticateType { LogIn, Register, GoogleSignIn }
enum ContianerType {
  HorizentalLayout,
  GridviewLayout,
  BannerLayout,
  SearchBar,
  Category
}
enum DiscountType {
  Flat,
  Percentage,
}
